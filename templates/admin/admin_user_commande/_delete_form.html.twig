<form method="post" action="{{ path('user_commande_delete', {'id': user_commande.id}) }}" onsubmit="return confirm('Êtes vous sûre de vouloir supprimé cette commande?');">
    <input type="hidden" name="_method" value="Supprimer">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user_commande.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
